## What changed

## Checks

- [ ] `npm run check` passes
- [ ] `npm run build:llms` was run when `data/` changed
- [ ] The CSP hash was updated when the inline JSON-LD changed
- [ ] The README file listing still matches the tree
